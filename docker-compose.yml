services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: sis
      POSTGRES_PASSWORD: sis_password
      POSTGRES_DB: sis
    ports:
      - "5432:5432"
    volumes:
      - sis_pg_data:/var/lib/postgresql/data

volumes:
  sis_pg_data: